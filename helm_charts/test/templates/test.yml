employee:
   #name1: {{ .Values.myname |  trim | quote -}}
   #name2:  {{- repeat 3 "hello" -}}
   name3: {{ randNumeric 3 }}
label:
      {{ include "test.labels" . | nindent 8 }}
containers:
{{ include "test.container" . }}  

sprec:
   containers:
   - name: {{ .Values.mycontainer1.name}}
     image: {{ .Values.mycontainer2.image}}
     ports:
     - containerPort: {{ .Values.mycontainer1.containerPort}}